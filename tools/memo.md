
- (続78補) **proj 真核の探索（修復ルートB予備調査）**: 反例 S=(7,1)(8,2)(7,1)
  で proj 0 (NT S)=D2(0)=NT([(8,2)])（**末尾再上昇 (7,1) を除外**）。だが
  仮説 msfx2（最大列+支配下サブrun）は **+1 で177違反**（S=(4,1)(5,2)(5,2)
  で同レベル継続 (5,2) を落とす — msfx2=[(5,2)] だが proj=D2(0)+D2(0)）。
  ⟹ **proj は単純な列セグメント（接尾/接頭/サブ）の NF に対応しない**。
  msfx は再上昇で過剰取得、msfx2 は同レベル継続で過少取得。項レベル射影
  （G-抽出不動点）は再上昇下で列セグメント像を持たない。**単純な msfx 置換
  は存在しない見込み** → ルートB（proj 真核発見）は困難。ルートA（¬olt のみで
  再構築・NT_tie_resolved 基盤）が有望。
